---

- name: Configure interfaces
  template:
    src: interfaces.j2
    dest: /etc/network/interfaces
    backup: true
    owner: root
    group: root
    mode: 0644
  become: true
  notify: Reboot Host
# check network configuration template
